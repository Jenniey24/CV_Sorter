/* config.js — set this once, right after you deploy apps-script.gs.
 *
 * 1. Deploy apps-script.gs as a Web App (Deploy > New deployment > Web app,
 *    Execute as: Me, Who has access: Anyone).
 * 2. Copy the URL it gives you (ends in /exec) and paste it below.
 * 3. Commit this file to your repo along with everything else.
 *
 * Safe to commit / make public: the URL alone can't read or write your
 * sheet. Every request also has to carry the console password's hash,
 * which is checked server-side in apps-script.gs — see auth.js.
 */
const APPS_SCRIPT_URL = "https://script.google.com/macros/s/AKfycbzI-YMpqh6gjM4svTvLql34GVGLjUCikBvyt04Hy4x5gscu7mBz7H47dJUexwICKMRe/exec";
